const ids = {
    Url: String(import.meta.env.VITE_URL),
    PId: String(import.meta.env.VITE_PID),
    DId: String(import.meta.env.VITE_DID),
    CId: String(import.meta.env.VITE_CID),
    BIdw: String(import.meta.env.VITE_BID),
    CIdb: String(import.meta.env.VITE_CIDB),
    CIdw: String(import.meta.env.VITE_CIDW),
    CIdc: String(import.meta.env.VITE_CIDC),
    CIdp: String(import.meta.env.VITE_CIDP),
    CIdca: String(import.meta.env.VITE_CIDCA),
}

export default ids